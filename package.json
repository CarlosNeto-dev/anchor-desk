{
  "name": "anchor-desk",
  "version": "1.0.0",
  "scripts": {
    "watch:scss": "sass --watch src/scss:dist/css",
    "watch:ts": "tsc --watch",
    "server": "live-server dist --port=8080",
    "dev": "npm run watch:scss & npm run watch:ts & npm run server",
    "build": "sass src/scss:dist/css && tsc"
},
  
  "devDependencies": {
    "sass": "^1.69.0",
    "typescript": "^5.3.0",
    "live-server": "^1.2.2"
  }
}
